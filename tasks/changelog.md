# Task Changelog

Dated history of meaningful task work and decisions.

## 2026-09-17

- Completed tickets 3 and 4 of `tasks/todo/tasks-lightweight-crm-ui.md`: Companies and People now have readable directories and canonical, data-backed hubs instead of route shells. Company hubs expose Overview, People, Meetings, Agent Sessions, and Knowledge; person hubs show their own context, optional Company, and explicitly linked history only.
- Reused existing loaders and shared `src/components/playground/CrmHubSections.tsx`; no duplicate fixtures, backend mutations, or alternate record URLs were introduced. Non-meeting internal notes/revenue context are separate from meeting documentation, assistant Call/Chat conversations are labeled separately, and absent company/history/context states are explicit.
- Added company/person-scoped Analysis placeholders with disabled actions and Sharing explanations linking only to `/settings/sharing`. No analysis runs, policy controls, or access grants exist. Agent Session detail layouts remain ticket 6; the Meeting overview-first redesign remains ticket 5.
- Kept hub sections visible rather than adding stateful tabs. Added semantic page/section headings and darker, always-underlined record links after accessibility review. The main scroll container resets on pathname changes so links from long hubs open at the destination overview rather than retaining an unrelated scroll offset.
- Added Playwright configuration and `tests/crm-hubs.spec.ts` via `npm run test:ui`, with browser setup documented in `README.md`. Validation: 9 browser tests and 17 backend tests passed, along with build/typecheck and independent code review. Coverage includes canonical navigation, person-specific membership, company-less Avery, empty states without DB mutation, placeholders, direct/reloaded URLs, browser back, sidebar highlighting, missing records, and scroll reset. Desktop screenshots were reviewed; no horizontal main-panel overflow at 1024px. Existing bundle-size and dependency-eval build warnings remain.
- Finalized tickets 3–4 with current task statuses, relevant files, glossary, and changelog. The overall task list remains in `tasks/todo/` while tickets 5–6 are pending; archive only after all parent tickets finish and the user approves.

- Completed ticket 2 of `tasks/todo/tasks-lightweight-crm-ui.md`: shared SQLite/API types for Companies, People, independent meeting participants, optional transcript/documentation artifacts, Call/Chat agent sessions, and company/person knowledge. Concept definitions are recorded in `glossary.md`.
- Seeded connected Halden history with person-specific participation, a customer call documented through a separate Phone Assistant call, an account-level Chat without a meeting link, Nordlicht/Mira as newly encountered static examples with voice-memo documentation, and Avery as a company-less contact. No creation, matching, recording, or assistant execution was added.
- Centralized canonical record queries in `server/records.ts` and connected Companies/People/Agent Sessions route loaders. Missing detail IDs now show a real 404 through React Router's error-response handling. These remain layout shells until tickets 3, 4, and 6.
- Separated meeting lifecycle (`scheduled`, `in_progress`, `held`, `cancelled`) from artifact state (`collecting`, `processing`, `ready`, `failed`). Meeting participants/counts no longer derive from transcript speakers. Existing Meeting/Calendar UI received compatibility changes and truthful artifact/provenance labels; ticket 5 still owns the overview-first redesign and full record links.
- Added transactional schema-v1 migration for the original seeded v0 database, preserving IDs, dates, summaries, transcript segments, invite metadata, and internal examples. Versioned initialization prevents reseeding on repeat startup. Custom multiple-meeting/calendar conflicts roll back with an explanation instead of silently merging records; equal-position transcript segments are preserved. Calendar entries and meetings remain distinct, with at most one canonical meeting per invite.
- Validation: `npm test` has 17 passing checks covering fresh initialization, upgrade preservation, rollback, idempotence, artifact/lifecycle conversion, transcript-independent participants, canonical API relationships, person-scoped history, optional links, filters, missing IDs, and rejected writes. A temporary copy of the existing v0 database retained all 9 meetings, 65 transcript segments, and 12 calendar entries after upgrade. Build/typecheck and Chrome smoke tests passed; no uncaught browser errors. Existing dependency eval and large-bundle build warnings remain.
- Finalization: ticket 1's previously uncommitted work was committed as `f90f7f2`. Ticket 2 checkboxes and relevant files are current. The overall task list remains in `tasks/todo/` because tickets 3–6 are pending; archive only after all parent tickets finish and the user approves.

- Completed ticket 1 of `tasks/todo/tasks-lightweight-crm-ui.md`: primary Companies, People, Meetings, and Agent Sessions navigation; retained Calendar; separated Team/Settings and the design-system utility.
- Added temporary list/detail shells in `src/routes/CrmRouteShell.tsx`, with explicit unconnected-data messaging, hub section headings, and canonical list-return links. No fake IDs or duplicate fixtures were introduced; loaders and record validation remain part of ticket 2. Existing Meetings and Calendar pages retain their loaders until their later tickets.
- Added shared Settings navigation and `/settings/sharing`, clearly labeled as a future capability with no policy controls or access-enforcement claims. Kept `/settings` redirecting to Account and `/` redirecting to Meetings.
- Validation: `npm run build` (including TypeScript) and Chrome/Playwright smoke checks passed for shell direct URLs, list-return links, browser back, exclusive sidebar highlighting, Settings navigation, retained destinations, seeded meeting detail, and the root redirect. Build reports dependency `lottie-web` eval and large-bundle warnings; no uncaught browser errors observed. Browser tooling ran from `/tmp/crm-nav-smoke` without adding project dependencies.
