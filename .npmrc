# @mui/x-date-pickers v6 still declares a React 17/18 peer range, so npm's strict
# peer resolution refuses a React 19 tree. pnpm only warns, which is why the
# monorepo runs this exact combination in production. Same stance here.
legacy-peer-deps=true
